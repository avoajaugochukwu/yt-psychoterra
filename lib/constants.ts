/**
 * Application Constants
 */

/**
 * Maximum number of unique images to generate per video.
 * Images will be randomly distributed across all scenes regardless of total scene count.
 * This caps generation costs at 60 × ~$0.004 = $0.24 per video.
 */
export const MAX_GENERATED_IMAGES = 60;
