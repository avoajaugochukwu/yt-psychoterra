// Development mode flag
export const IS_DEVELOPMENT = process.env.NODE_ENV === "development";

// Scene duration for video production (in seconds)
export const SCENE_DURATION_SECONDS = 7; // Each scene displays for 7 seconds
